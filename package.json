{
  "name": "mongo-driver",
  "version": "1.0.0",
  "description": "basic node app to connect to mongodb local instance using mongodb driver",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "soumya",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongodb": "^4.9.1"
  }
}
